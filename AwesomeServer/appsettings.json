{
  "ProjectName": "Awesome!",
  "DbConnection": "Filename=awesome.db",
  "LogLevel": "Trace"
  // In the default appsettings.json file (used in development environment) for EmailOptions
  // and Secret we use user secrets
}